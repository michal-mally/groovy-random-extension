apply plugin: 'idea'
apply plugin: 'maven'
apply plugin: 'groovy'

group   = 'pl.helenium.showcase'
version = '0.1.0-SNAPSHOT'

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    groovy group: 'org.codehaus.groovy', name: 'groovy', version: '2.+'
}

test {
    useTestNG()
}
